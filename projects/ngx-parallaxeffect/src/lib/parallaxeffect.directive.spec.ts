import { ParallaxeffectDirective } from './parallaxeffect.directive';

describe('ParallaxeffectDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxeffectDirective();
    expect(directive).toBeTruthy();
  });
});
